server:
  port: 8087

spring:
  application:
    name: seckill-mongo
  data:
    mongodb:
      uri: mongodb://localhost:27017/movie

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core: DEBUG
